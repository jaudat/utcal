<p id="notice"><%= notice %></p>


<%= javascript_include_tag "calendarBuilder.js" %>
<script type="text/javascript">

$(document).ready(function(){

	<% @courses.each do |course| %>
	<% course.start = course.start.to_time.to_i %>
	<% course.end = course.end.to_time.to_i %>
	<%end%>

	var coursesToJSON = <%= raw @courses.to_json %> 
	buildCalendar(coursesToJSON);

});
    
</script>

<p>Check course schedules for: </p><br />
<% @courses.each do |course| %>
	<%= link_to course.code, users_mystudents_courses_path(course.id) %><br />
<%end%>


<div id='calendar'></div>

<p class="test">
  <b>First name:</b>
  <%= @professor.f_name %>
</p>

<p>
  <b>Last name:</b>
  <%= @professor.l_name %>	
</p>

<p>
<b> List of courses </b>
 <%= @course %>  

</p>

<%= link_to 'Edit', edit_professor_path(@professor) %> |
<%= link_to 'Back', professors_path %>

<% @courses.each do |course| %>
<%= course.to_json %>
<% end %>
